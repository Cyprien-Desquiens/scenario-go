apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ing-ws-cpt-go
spec:
  rules:
    - http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: ws-cpt-go
                port:
                  number: 80